const char *removeFiles[] = {"/tmp/removefile"}; // Files silk-guardian will remove upon change in usb state.
const char *shredIterations = "3"; // How many times to shred file. The more iterations the longer it takes.
